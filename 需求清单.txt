创建一个销售订单
    原有的创建逻辑是定义一个订单类，然后然订单类的信息去填写属性值，并创建这个实体类。
    而实际的业务中可能订单是一个很复杂的类，需要很多的视图子类组装而成。
    例如：销售订单存在基础数据视图、销售视图、订单行项目视图、装运视图、发票视图、科目分配视图、订单状态视图、计划行视图


创建缓存使用机制
    缓存的使用是为了解决数据库压力过大
    访问数据时先访问缓存中的数据，如果缓存中没有就从数据库中取。取出后把数据添加到缓存中。
